// Re-export toast utilities from the Toast component
// This provides a cleaner import path: import { useToast, toast } from '@/lib/toast'

export { useToast, toast, ToastProvider, setGlobalToast } from '@/components/ui/Toast';
